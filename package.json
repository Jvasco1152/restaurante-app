{
  "name": "restaurante-app",
  "version": "1.0.0",
  "description": "Sistema de reservas y menú digital para restaurantes",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:db\" \"npm run dev:wait\" \"npm run dev:client\"",
    "dev:db": "cd server && npx prisma dev",
    "dev:wait": "npm run wait-for-db --workspace=server && npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "install:all": "npm install && npm install --workspace=client && npm install --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
